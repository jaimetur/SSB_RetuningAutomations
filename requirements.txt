# Core dependencies
pandas==2.2.3
numpy>=1.24.0
openpyxl>=3.1.5
pillow>=11.2.1
colorama>=0.4.6
python-pptx

# Optional (recommended for best performance and CLI experience)
python-dateutil>=2.8.2

# Only necesary to generate User_Guide
python-docx>=1.1.2
markdown>=3.6
reportlab>=4.2.2

# Only necesary to generate the Bineries files (it is done by Github)
pyinstaller>=6.0.0
nuitka>=2.7.3


# Web Interface (not necesary if you use docker to launch the web service)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.4
python-multipart>=0.0.9
passlib>=1.7.4
